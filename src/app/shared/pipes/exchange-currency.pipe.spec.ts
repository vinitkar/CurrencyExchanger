import { ExchangeCurrencyPipe } from './exchange-currency.pipe';

describe('ExchangeCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ExchangeCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
